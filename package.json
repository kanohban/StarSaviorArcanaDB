{
  "version": "6.5.2",
  "dependencies": {
    "prettier": "^3.7.4",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "deploy": "git push origin gh-pages",
    "data:export": "node scripts/manage_data.js export",
    "data:import": "node scripts/manage_data.js import"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}